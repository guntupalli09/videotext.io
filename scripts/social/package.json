{
  "name": "videotext-social-cards",
  "private": true,
  "description": "Renders branded VideoText social-post cards (PNG) via headless Chromium — no external image-gen API needed.",
  "type": "module",
  "scripts": {
    "render": "node render-card.mjs"
  },
  "dependencies": {
    "playwright": "^1.56.1"
  }
}
